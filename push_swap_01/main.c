#include <stdio.h>
#include"push_swap/push_swap.h"
/*

int main() {
    t_list a;
    t_list b;
    int i = 0;
    a.pile= malloc(20);
    b.pile = malloc(20);
    while(i<10)
    {
        a.pile[i] = i + 48;
        b.pile[i] = i + 48;
        i++;
    }
    a.pile[i] = '\0';
    b.pile[i] = '\0';
    b.len[0] = 10;
    a.len[0] = 10;
    //sa(a, b);
    printf("%s\n", b.pile);
    sa(a, b);
    printf("\n%s\n%d\n%s\n%d",a.pile, a.len, b.pile, b.len);
    return 0;
    //trouver le bon systeme pour la len
}
*/

/*int main (void)
{
    char *s[]={"0","-1","2","3","-4548",};
    t_node *a;
    t_node *b;
    fill_node(&b,s,4);
    fill_node(&a,s,4);
    //printlinkedlist(&a, &b);
    printf("\n%d", is_sorted(&a));
}
*/